# Requirements for Client Health Voice Agent
# Optimized for Hugging Face Spaces deployment
# Note: Streamlit handles audio via browser, so no system audio libraries needed

# Web Framework
streamlit>=1.33.0
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Speech-to-Text
faster-whisper==1.0.3
torch==2.1.2

# AI/LLM
google-generativeai==0.3.0

# Audio Processing (minimal - soundfile needed for temp file handling)
soundfile==0.12.1
numpy==1.24.3

# Text-to-Speech (Multiple engines for reliability)
edge-tts==6.1.9
gTTS==2.5.1

# Utilities
requests==2.31.0
python-dotenv==1.0.0
pydantic==2.5.3
